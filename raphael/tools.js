/**
 * @author xiaxianlin
 */
!(function() {
    var ele = document.getElementById('tools'),
        w = ele.offsetWidth - 2,
        h = ele.offsetHeight - 36,
        paper = new Raphael('toolCanvas', w, h);
}());
